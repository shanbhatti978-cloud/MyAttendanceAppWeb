<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>RAMS - Admin Web Dashboard</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>

  <!-- ===================== LOGIN SCREEN ===================== -->
  <div id="loginScreen" class="login-wrapper">
    <div class="login-card">
      <div class="login-icon">🏭</div>
      <h1>Reliance Attendance Management System</h1>
      <p class="subtitle">Admin Web Dashboard</p>
      <form id="loginForm">
        <input type="text" id="loginUsername" placeholder="Username" autocomplete="username" required />
        <input type="password" id="loginPassword" placeholder="Password" autocomplete="current-password" required />
        <button type="submit" id="loginBtn">LOGIN</button>
        <div id="loginError" class="error-text"></div>
      </form>
      <p class="hint">Use the same Admin or Supervisor login you use on the mobile app.<br>
      (Make sure that account has been synced at least once from the app: Settings &gt; Cloud Sync &gt; Sync Now.)</p>
    </div>
  </div>

  <!-- ===================== MAIN APP ===================== -->
  <div id="app" class="app-shell hidden">
    <header class="topbar">
      <div class="topbar-left">
        <span class="logo">🏭 RAMS</span>
        <span class="env-badge">Web Dashboard</span>
      </div>
      <div class="topbar-right">
        <span id="userLabel"></span>
        <button id="logoutBtn" class="btn-outline">Logout</button>
      </div>
    </header>

    <div class="layout">
      <nav class="sidebar">
        <button class="nav-item active" data-view="dashboard">📊 Dashboard</button>
        <button class="nav-item" data-view="employees">👥 Employees</button>
        <button class="nav-item" data-view="attendance">✅ Attendance</button>
        <button class="nav-item" data-view="leave">📝 Leave Requests</button>
        <button class="nav-item" data-view="reports">📈 Reports</button>
      </nav>

      <main class="content">

        <!-- DASHBOARD VIEW -->
        <section id="view-dashboard" class="view">
          <h2>Today's Overview</h2>
          <div class="kpi-grid" id="kpiGrid"></div>
          <h3>Recent Leave Requests</h3>
          <div id="recentLeaveList" class="card-list"></div>
        </section>

        <!-- EMPLOYEES VIEW -->
        <section id="view-employees" class="view hidden">
          <h2>Employees</h2>
          <input type="text" id="employeeSearch" class="search-box" placeholder="Search by name, code, or designation..." />
          <div id="employeeTableWrap"></div>
        </section>

        <!-- ATTENDANCE VIEW -->
        <section id="view-attendance" class="view hidden">
          <h2>Attendance by Date</h2>
          <div class="filter-row">
            <input type="date" id="attendanceDate" />
            <button id="attendanceLoadBtn" class="btn-primary">Load</button>
          </div>
          <div class="kpi-grid" id="attendanceKpi"></div>
          <div id="attendanceTableWrap"></div>
        </section>

        <!-- LEAVE REQUESTS VIEW -->
        <section id="view-leave" class="view hidden">
          <h2>Leave Requests</h2>
          <div class="filter-row">
            <select id="leaveStatusFilter">
              <option value="Pending">Pending</option>
              <option value="Approved">Approved</option>
              <option value="Rejected">Rejected</option>
              <option value="All">All</option>
            </select>
          </div>
          <div id="leaveTableWrap"></div>
        </section>

        <!-- REPORTS VIEW -->
        <section id="view-reports" class="view hidden">
          <h2>Monthly Report</h2>
          <div class="filter-row">
            <input type="month" id="reportMonth" />
            <button id="reportLoadBtn" class="btn-primary">Load</button>
          </div>
          <div id="reportTableWrap"></div>
        </section>

      </main>
    </div>
  </div>

  <div id="toast" class="toast hidden"></div>

  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.min.js"></script>
  <script src="supabase-config.js"></script>
  <script src="app.js"></script>
</body>
</html>
